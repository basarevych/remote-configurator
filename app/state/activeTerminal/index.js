import activeTerminalReducer from "./reducers";

import * as activeTerminalTypes from "./types";
import * as activeTerminalOperations from "./operations";
import * as activeTerminalSelectors from "./selectors";

export {
  activeTerminalTypes,
  activeTerminalOperations,
  activeTerminalSelectors
};
export default activeTerminalReducer;
